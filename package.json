{
  "name": "discover",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ nanspro/discover",
    "create-local": "graph create --node http://localhost:8020/ nanspro/discover",
    "remove-local": "graph remove --node http://localhost:8020/ nanspro/discover",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nanspro/discover"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.13.3",
    "@graphprotocol/graph-ts": "0.15.1"
  }
}
